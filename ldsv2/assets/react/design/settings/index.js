import design from './components/design';
import pattern from './components/pattern';

const Settings = {
    Design: design,
    Pattern: pattern,
};

export default Settings;

export const {
    Design,
    Pattern,
} = Settings;
