export const StateName = {
    NOTIFICATION: 'notification',
}

const prefix = 'notification_';

export const Action = {
    RESET: prefix + 'reset',
};

export const MessageType = {
    GROUP_INVITATION: 'App\\Notifications\\GroupInvitation',
}
