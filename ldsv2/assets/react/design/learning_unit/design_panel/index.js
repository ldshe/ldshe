import unit from './containers/unit';
import pattern from './containers/pattern';

const DesignPanel = {
    Unit: unit,
    Pattern: pattern,
};

export default DesignPanel;

export const {
    Unit,
    Pattern,
} = DesignPanel;
